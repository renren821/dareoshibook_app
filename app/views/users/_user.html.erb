<div class="item">
  <%= link_to user.name, user, class: 'link' %>
  <% if current_user.admin? && !current_user?(user) %>
    <%= link_to "delete", user, method: :delete, data: { confirm: "本当に削除してよろしいですか？" }, class: 'link' %>
  <% end %>
</div>
