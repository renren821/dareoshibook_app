<% if @feed_items.any? %>
  <%= will_paginate @feed_items, previous_label: '&lt', next_label: '&gt', inner_window: 1, outer_window: 0 %>
  <ul class="item-list">
    <%= render @feed_items %>
  </ul>
  <%= will_paginate @feed_items, previous_label: '&lt', next_label: '&gt', inner_window: 1, outer_window: 0 %>
<% end %>